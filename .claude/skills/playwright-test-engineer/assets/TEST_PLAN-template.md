# Playwright test plan — `<package-name>`

Generated by the `playwright-test-engineer` skill. This is a working document to confirm scope before implementation — it documents this skill's own convention (the repo has no pre-existing plan-file format) and is not required to be committed.

- **Package**: `packages/<package-name>` (or a downstream `packages-bamoe/<name>` / `packages-bamoe-artifacts/<name>`)
- **How it's served**: <storybook iframe story | full application dev server | not yet set up>
- **Existing `tests-e2e/`**: <summary — file count, feature groups, fixtures found, or "none — bootstrapping">

## Scenarios

Repeat this block per confirmed scenario.

### `<NN>` — `<Scenario name>`

- **Description**: <one or two sentences, plain language>
- **Covers**: `<path/to/source/file.tsx>` — <what feature/code path this exercises>
- **Approach**: E2E via <storybook story id | app route>; <needs a screenshot assertion? yes/no and why>
- **Fixtures/utilities used**: `<fixtureName>` (existing, from `tests-e2e/__fixtures__/<file>.ts`) | `<fixtureName>` (new — will be added to `base.ts`) | cross-package: `@kie-tools/<other-package>/tests-e2e/__fixtures__/<file>`
- **Assertions / acceptance criteria**:
  - <functional assertion, e.g. "node X is attached with name Y">
  - <model assertion, e.g. "underlying JSON model's drgElements[0] equals {...}">
  - <screenshot assertion, if applicable, with the planned file name>
- **Prerequisite state / setup**: <e.g. "editor open with default empty diagram via `editor.open()`", or a specific fixture DRD/model that must be loaded first>
- **File**: `tests-e2e/<featureGroup>/<camelCaseScenarioName>.spec.ts`

---

## New fixtures required (if any)

List any page objects that don't exist yet and will be added to `__fixtures__/` and wired into `base.ts`, with what methods they need.

## Open questions / risks

Anything ambiguous that needs a decision before implementation (e.g. a scenario that could belong to a different package, a missing base config value, a feature with no storybook story to drive it).
